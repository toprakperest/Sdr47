# Core Dependencies
numpy
scipy
psutil
pyzmq
pydantic
structlog
opentelemetry-api
opentelemetry-sdk
scikit-learn
matplotlib

# SDR Hardware Interface (Requires SoapySDR core library installed on the system)
# Installation might vary depending on OS and SDR hardware.
# See SoapySDR documentation: https://github.com/pothosware/SoapySDR/wiki
# Example pip install (might not be sufficient): pip install SoapySDR
# Consider installing via package manager (apt, brew, conda) if available.
# SoapySDR

# Optional Visualization Libraries (Install based on chosen backend)
# plotly
# pyvista
# vtk

# Optional GUI Libraries (Install if using PyQt interface)
# PyQt5
# pyqtgraph

